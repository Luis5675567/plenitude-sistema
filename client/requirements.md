## Packages
date-fns | For date parsing, formatting and calculating status badge differences
framer-motion | For smooth page transitions and micro-interactions
lucide-react | Already in stack, using for modern icons

## Notes
- Images uploaded go to /api/upload and return { url: string }
- Authentication checks GET /api/me and expects 200 or 401
- Sidebar UI uses standard layout primitives, routing relies on wouter
- Ensure strict dark mode for elite modern gym vibe
